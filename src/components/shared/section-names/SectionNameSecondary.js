import React from "react";

const SectionNameSecondary = ({ children }) => {
  return (
    <span className="text-size-15 font-semibold text-secondaryColor inline-block uppercase mb-[13px] tracking-[1px]">
      {children}
    </span>
  );
};

export default SectionNameSecondary;
